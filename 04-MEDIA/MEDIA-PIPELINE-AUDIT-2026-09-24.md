# Media Pipeline Audit — 2026-09-24

## Inputs reviewed

Two founder-provided terminal transcripts were inspected:
- first materialization under the conservative 149/11 rights state;
- second materialization under the updated 156/4 founder-decision state.

## Finding 1 — stale derivatives after rerun

Both successful runs targeted the same directory:
`04-MEDIA/library/master-v1/`

The earlier builder did not clear or atomically replace the output tree.

Exact manifest-path comparison:
- old allowed outputs: 149
- new allowed outputs: 156
- output filenames unchanged/overwritten by both runs: 24
- old output filenames not regenerated by the second run: 125

Therefore, if no manual cleanup happened between the two runs, the directory can contain 281 files rather than 156.

One stale file is the now-rejected other-identity BTS frame:
`capability-bts-01/0025-g0028-s01-359778954_17974354778454137_5337717606211314694_n.jpg`

The builder has been fixed to use a fresh staging tree and replace the old generated tree only after a zero-error build.

## Finding 2 — Wave 01 pack builder schema mismatch

The old `tools/make_run_media_packs.py` expected `design_set_affinity`.

The audited M001/normalized schema does not contain that field.

Without correction, every item defaulted to `shared`, causing A/B/C/D to collapse toward identical full-library packs.

The script has been replaced with explicit family allowlists.

Expected pack counts from the current normalized 156-item local materialization set:
- shared: 44 images
- A: 81 images
- B: 60 items = 51 images + 9 videos
- C: 76 items = 67 images + 9 videos
- D: 65 items = 56 images + 9 videos

## Gate

Do not commit or publish the current generated media directory until:
1. the fixed clean builder is pulled;
2. a clean rebuild reports 156 / 4 / 0;
3. filesystem count is exactly 156;
4. no G0028/s01 generated derivative exists;
5. pack generation reports the expected bounded counts.

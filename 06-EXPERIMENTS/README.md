# Experiments

Each run gets a monotonically increasing experiment ID and its own folder.

A single model may have many runs.

Do not create a Git branch for every Chat.Z.AI run. Keep run specifications on `main`; create real implementation branches only after a candidate has been externally audited and selected for continuation.

# End-to-End Product Development Pipeline V5

Date: 2026-09-25
Status: CURRENT_OPERATING_PIPELINE

Supersedes V4 for NEW work.

Core ordering change:
official E004–E011/Q001 launch occurs AFTER pre-Astra finalization, Astra R001, and accepted Architecture V4.

## Principle

No later stage may silently compensate for an earlier failed gate.

Every stage has:
- explicit inputs;
- explicit outputs;
- binary checks;
- stop/fallback semantics.

## Stage 0 — authority/source lock

Inputs:
- 2026-09-25 founder decisions;
- current public supersession;
- current private source audit head;
- verified OS23.6 source/release evidence;
- historical site anchors.

Output:
one current authority snapshot.

PASS:
no current routing points new work to stale launch strategy.

## Stage 1 — central architecture closure

Close every technical decision possible without Astra.

Already closed:
- modular monolith;
- application/use-case layer;
- A1 execution substrate;
- A2 creative revision semantics;
- A5 auth/MCP/secrets;
- A6 staged migration;
- persistence/lineage/export/audit baselines.

Provisional residual:
- K1 model/profile/provider-deployment identity boundary;
- K2 non-idempotent ambiguous paid-submit behavior.

Output:
minimized open-knot set.

## Stage 2 — Wave v4 media/transport preparation

Local/private source:
clean master + explicit A/B/C/D group/slot curation.

Build:
- A39;
- B42;
- C33;
- D36;
- sanitized re-encoded assets;
- source-path-free manifests;
- exact hashes.

Then:
- verify_public_wave;
- actual-transport contact sheets;
- visual review every tile/poster;
- public-lab audit.

Output:
sanitized transport ready for later official wave.

Important:
transport PASS does NOT authorize E/Q launch.

## Stage 3 — P001–P004 evidence collection

As pilots finish:
- preserve ZIP;
- SHA-256;
- actual runtime label;
- source-tree audit;
- build/test evidence;
- architecture/persistence/MCP/generation analysis.

Output:
P00X forensic audits + combined architecture evidence.

Do not wait indefinitely for a pilot that cannot materially affect K1/K2.

## Stage 4 — public-history privacy strategy

Keep old pinned commits accessible for historical pilots.

Before long-lived official bridge:
create sanitized clean-history bridge/root or equivalent.

Output:
privacy-safe official transport strategy.

## Stage 5 — pre-Astra synthesis

Create compact:
- executive snapshot;
- source behavior matrix;
- architecture delta map;
- final open knots;
- evidence router;
- updated ADRs/nonnegotiables/project memory.

No long chat transcript.
No irrelevant design/media detail.
No solved questions.

## Stage 6 — two pre-Astra audits

Structural:
paths, SHAs, authority, stale docs, launch guards, Git privacy boundary.

Semantic/adversarial:
model drift, duplicate paid submit, event races, cancellation, secret leakage, MCP authorization, migration divergence, historical provenance.

PASS/PASS required.

## Stage 7 — immutable Astra freeze + R001

Create one immutable full 40-char SHA.

Then:
GPT-6 Astra / XHIGH / READ_ONLY.

Astra resolves only final irreducible knots.

Preserve raw output unchanged.

## Stage 8 — Architecture V4 reconciliation

Compare:
central baseline
vs Astra
vs verified source
vs pilot evidence.

For each delta:
KEEP / CHANGE / REJECT / NEEDS_FOUNDER_DECISION / NEEDS_EVIDENCE.

Publish:
- Architecture V4;
- Domain V4 if required;
- run-contract V4;
- migration sequence;
- accepted ADR updates.

## Stage 9 — official E/Q freeze

Bind:
- Architecture V4;
- current source contract;
- sanitized Wave v4 media;
- final reference recipes;
- E004–E011/Q001 run specs.

Set statuses:
READY

Set launch gate:
ARCHITECTURE_V4_FROZEN

Create:
OFFICIAL-LAUNCH-FREEZE-V4.md = OFFICIAL_WAVE_READY.

Commit/push.
Record exact SHA.
No mutation after freeze.

## Stage 10 — launch prompt generation

Only the fail-closed generator may generate prompts.

It verifies:
- checked-out HEAD == requested SHA;
- clean tracked tree;
- OFFICIAL-LAUNCH-FREEZE-V4 exists;
- OFFICIAL_WAVE_READY;
- every run STATUS READY;
- every run gate ARCHITECTURE_V4_FROZEN.

## Stage 11 — isolated parallel launch

E004/E005 A
E006/E007 B
E008/E009 C
E010/E011 D
Q001 qualification

Rules:
- unique chat;
- unique sandbox;
- GitHub read-only;
- exact frozen SHA;
- actual model label recorded;
- unique ZIP;
- no sibling implementation import.

## Stage 12 — collection + forensic audit

For every run:
- ZIP bytes/hash;
- source tree;
- build/lint/tests;
- source reconciliation;
- architecture/domain;
- provider truth;
- graph/use-case parity;
- persistence/auth/MCP;
- responsive/i18n/a11y;
- media/provenance;
- screenshots;
- failures.

## Stage 13 — synthesis

Promote bounded units only:
contract/module/component/design/interaction/copy/IA/provider-independent code.

Never merge a whole candidate automatically.

## Stage 14 — canonical private promotion

Only after founder selection:
public experiment evidence
→ dedicated private canonical branch
→ canonical tests/security/migrations
→ review
→ deliberate integration/deployment.

## Current state

Stages 0–1: advanced; A1/A2/A5/A6 closed.
Stage 2: requires local SSD execution.
Stage 3: waiting on P001–P004 outputs.
Stages 4–6: pending.
Stage 7: blocked.
Stages 8–14: blocked.
